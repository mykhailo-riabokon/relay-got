import characters from './characters.query';
import toggleCharacter from './toggleCharacter.mutation';

const queries = {
  characters,
};

const mutations = {
  toggleCharacter,
};

export default {
  queries,
  mutations,
};
